// import { Router } from "express";
// import { verifyJWT } from "../middlewares/auth.middleware.js";
// import { upload } from "../middlewares/multer.middleware.js";
// import { deleteImage, uploadImage, editImage } from "../utils/ImageHandling.js";
// const router = Router();

// router
//   .route("/upload-image")
//   .post(verifyJWT, upload.single("image"), uploadImage);

// router.route("/delete-image/:publicId").delete(deleteImage);
// router.route("/edit-image/:publicId").put(upload.single("image"), editImage);
// export default router;
